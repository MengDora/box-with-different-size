import bpy
import random

c = bpy.context
d = bpy.data


for i in range(30):
    e = random.randint(2,9)
    bpy.ops.mesh.primitive_cube_add(location = [ i*3,e,e])
    obj = c.active_object
    if i%4 == 0:
        obj.scale = [e,e,e]
        
print("----" * 10)